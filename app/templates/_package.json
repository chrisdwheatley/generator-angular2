{
  "name": "package",
  "version": "0.0.0",
  "dependencies": {},
  "devDependencies": {
    "angular2": "^2",<% if (transpiler === 'babel') { %>
    "<%= transpiler %>": "^4"<% } else if (transpiler === 'traceur') { %>
    "<%= transpiler %>": "^0"<% } %>
  }
}
